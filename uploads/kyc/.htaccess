# KYC demat / CMR proofs — never serve statically.
# Stream only via authenticated viewKycProof API action.
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
Options -Indexes

<IfModule mod_headers.c>
  Header set Cache-Control "private, no-store"
</IfModule>
